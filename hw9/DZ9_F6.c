/*
Сдать решение задачи F6-Два одинаковых
Два одинаковых
Написать только одну логическую функцию, которая определяет, верно ли, что среди элементов массива есть два одинаковых.
Если ответ «да», функция возвращает 1;
если ответ «нет», то 0.
Строго согласно прототипу:
int is_two_same(int size, int a[]);

Формат входных данных
Массив из целых чисел
Формат результата
Функция возвращает 1 или 0

*/

#include <stdio.h>  // подключаем заголовочный файл stdio.h
#include <stdint.h> // полключение заголовочного файла типы переменных.
#include <string.h>
#define SIZE 100
int is_two_same(int size, int a[]) {
    // Перебираем все элементы массива
    for (int i = 0; i < size - 1; i++) {
        // Сравниваем текущий элемент с последующими
        for (int j = i + 1; j < size; j++) {
            if (a[i] == a[j]) {
                // Нашли пару одинаковых элементов
                return 1;
            }
        }
    }
    // Если не нашли ни одной пары, возвращаем 0
    return 0;
}

/*
int main()
{

    int array1[] = {3, 5, 7, 3}; // есть пара '3'
    int array2[] = {1, 2, 3, 4}; // нет пар
    int array3[] = {9, 9, 9};    // три одинаковых элемента
    
    printf("array1: %d\n", is_two_same(4, array1)); // ожидается: 1
    printf("array2: %d\n", is_two_same(4, array2)); // ожидается: 0
    printf("array3: %d\n", is_two_same(3, array3)); // ожидается: 1 (т.к. есть хотя бы одна пара)
    
    return 0;
}
*/